public class Circulo {
    void calcularArea() {
    double radio = 3.5; // Ingresar el valor del radio del círculo
    double area = Math.PI * Math.pow(radio, 2);
    System.out.println("El área del círculo es: " + area);
    }
}
